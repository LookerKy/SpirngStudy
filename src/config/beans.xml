<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- namespace에서 context 선택 후 properties파일 경로 설정 -->
	<context:property-placeholder location="classpath:config/value.properties"/>
	
	<bean id="hello2" class="springstudy.di.xml.Hello">
		<!-- index 속성 대신 name 속성을 가지고 사용할 수 도있다 이때 name 은 파라미터의 변수 명으로 사용하면 된다. -->
		<constructor-arg index="0" value="${myname}"/>
		<constructor-arg index="1" ref="${myprinter}"/>
		<property name="names">
			<list>
				<value>${value1}</value>
				<value>${value2}</value>
				<value>${value3}</value>
			</list>
		</property>
	</bean>
	
	<bean id ="hello" class="springstudy.di.xml.Hello">
		<!--setName() method 와 mapping -->
		<property name="name" value="Spring"/>
		<!--setPrinter() method 와 mapping -->
		<property name="printer" ref="printer"/>
		
		<property name="names">
			<list>
				<value>AOP</value>
				<value>Spring</value>
				<value>DI</value>
			</list>
		</property>
	</bean>	
	
	<!-- 1. 먼저 불러올 클래스 생성 -->
	<bean id = "printer" class="springstudy.di.xml.StringPrinter"/>
	<bean id = "consolePrinter" class="springstudy.di.xml.ConsolePrinter"/>

</beans>
